name: Renovate Plugin Pins

on:
  workflow_dispatch:

jobs:
  renovate:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        repo:
          - antigravity
          - claude
          - codex
          - gemini
          - openclaw
          - opencode
          - zed
    steps:
      - uses: actions/create-github-app-token@v1
        id: app-token
        with:
          app-id: ${{ vars.COREE_BOT_CLIENT_ID }}
          private-key: ${{ secrets.COREE_BOT_PRIVATE_KEY }}
          owner: coree-ai
          repositories: ${{ matrix.repo }}

      - uses: actions/checkout@v6
        with:
          repository: coree-ai/${{ matrix.repo }}
          token: ${{ steps.app-token.outputs.token }}

      - uses: renovatebot/github-action@v46.1.15
        with:
          token: ${{ steps.app-token.outputs.token }}
          renovate-version: full
        env:
          # Kept at debug intentionally: pin-propagation issues are hard to
          # diagnose at info level (customManager extraction, postUpgradeTasks).
          LOG_LEVEL: debug
          RENOVATE_REPOSITORIES: coree-ai/${{ matrix.repo }}
          # TEMPORARY: recreate the coree-pin PRs that were closed during setup.
          # Remove once the 0.15.0 pin PRs are open in all repos.
          RENOVATE_RECREATE_WHEN: always
          # Allow the postUpgradeTasks command (lockfile + plugin own-version regen)
          RENOVATE_ALLOWED_COMMANDS: '["^node scripts/renovate-post-upgrade\\.mjs .+$"]'
