#!/usr/bin/env bash

eval "$(devenv direnvrc)"
use devenv

# Load secrets from .env (gitignored)
dotenv_if_exists

# Use the locally built release binary so the terminal matches the plugin
PATH_add target/release
