/target

# Claude Code local-only state (settings.json is committed, local overrides are not)
.claude/*.local.json
.claude/*.local.md

# Devenv
.devenv*
devenv.local.nix
devenv.local.yaml

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

.memso/

# Local secrets (never commit)
.env
